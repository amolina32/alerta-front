import { TextFieldTypes } from "@ionic/core";

export const SHARED_CONSTANTS = {
  VALUE_TYPES: {
    TEXT: "text" as TextFieldTypes,
    PASSWORD: "password" as TextFieldTypes,
  },
  STORAGE_KEYS: {
    TOKEN: "token",
  },
};
